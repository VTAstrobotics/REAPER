name: Workflow 2

on:
  workflow_run:
    workflows: [Workflow 1]
    types:
      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Workflow 2 triggered successfully!"
